'use strict';

var gulp = require('gulp'),
	htmllint = require('../src/index.js');

// default task
gulp.task('default', function() {
	return gulp.src('./*.html')
		.pipe(htmllint({
			'failOnError': true
		}))
		.on('error', function() {});
});
